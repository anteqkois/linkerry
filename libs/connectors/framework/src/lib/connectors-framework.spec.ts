import { connectorsFramework } from './connectors-framework'

describe('connectorsFramework', () => {
  it('should work', () => {
    expect(connectorsFramework()).toEqual('connectors-framework')
  })
})
